package main // Package declaration, every Go program must start with it.

import "fmt" // shorthand for format, implements formatting for I/O

func main(){
    // Access man pages: `go doc fmt.Println`
    fmt.Println("Hello, World") // If Go doesn't even use semi-colons, why not permit printing with "print(...)" -_-
}
